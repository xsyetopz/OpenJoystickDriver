{
  "positive": [
    "Repair an existing HID parser behavior without changing its owner.",
    "Change a Kit protocol contract while preserving the app boundary.",
    "Update a controller catalog input and regenerate the canonical record.",
    "Change authored DriverKit relay configuration without editing .build output.",
    "Repair an existing RPC or remapping behavior at its current package seam."
  ],
  "negative": [
    "Move Sources and matching Tests into a new capability directory.",
    "Add a product-only behavior test and run the repository gates.",
    "Design the macOS menu-bar settings flow and accessibility states.",
    "Design an unrelated Swift package with no OpenJoystickDriver files.",
    "Edit generated .build/driverkit/generated files directly.",
    "Add unit tests for a standalone shell or Python script.",
    "Give generic Apple HIG advice without changing this repository."
  ]
}
