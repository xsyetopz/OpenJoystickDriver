{
  "positive": [
    "Import a new Xbox VID/PID from the pinned Linux xpad source.",
    "Add a local controller override with captured endpoint evidence.",
    "Patch a generated controller record through ControllerOverrides and regenerate.",
    "Validate catalog provenance, schemas, decimal JSON, and profile output.",
    "Regenerate the catalog after an intentional lockfile or override change."
  ],
  "negative": [
    "Capture and interpret live USB packets from a physical controller.",
    "Fix a parser implementation without changing catalog data.",
    "Add a Swift behavior test for an existing controller feature.",
    "Move controller Sources and Tests into a new capability directory.",
    "Design the controller settings screen and VoiceOver flow.",
    "Edit a generated Controllers JSON file directly."
  ]
}
