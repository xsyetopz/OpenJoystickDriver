{
  "positive": [
    "Add a Swift behavior test for a parser and run the macOS 14 parser harness.",
    "Prove a CLI route through its product API without asserting help prose.",
    "Validate a controller record with catalog and profile checks, then run focused tests.",
    "Run the product-only gate sequence after a runtime or remapping change.",
    "Report an unavailable DriverKit or hardware check with its exact command and blocker."
  ],
  "negative": [
    "Add a Swift test that reads scripts/ojd or a Swift source file and matches substrings.",
    "Create a Tests/Scripts fixture for a shell or Python helper.",
    "Move capabilities or split source directories without routing topology to organize-openjoystickdriver.",
    "Redesign the settings window or perform a VoiceOver review without routing UI work.",
    "Edit .build/driverkit/generated output or add a local validator script."
  ]
}
