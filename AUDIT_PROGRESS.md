# Regression Audit Progress Book

Updated: 2026-06-13 04:33 EEST

Objective: audit and resolve regressions affecting UI lag, controller behavior, daemon health,
permission prompts/state, GUI state, stale installed contents, and duplicate virtual devices.

## Audit method

Each row must have:
- Requirement: user-visible invariant to protect.
- Evidence: command, test, runtime observation, or source location that proves current state.
- Status: Pass, Fixed, Blocked, or Open.
- Next action: concrete follow-up if not fully proven.

## Progress ledger

| Area | Requirement | Evidence | Status | Next action |
| --- | --- | --- | --- | --- |
| Stale GUI contents | `rebuild-fast` must not leave an already-running GUI process using the old app bundle. | Added `ScriptPackagingTests.testRebuildFastQuitsRunningGuiBeforeReplacingInstalledApp`; `./scripts/ojd rebuild-fast dev` printed `Quitting running OpenJoystickDriver GUI...`; process check showed new GUI PID `97549` replacing old PID `63911`. | Fixed | Recheck after any rebuild script edits. |
| Duplicate virtual devices | Compatibility mode must expose one user-space virtual gamepad, not one per consumer. | Fresh after GetReport patch: `rtk ./scripts/ojd diag backends --seconds 2` found exactly 1 OJD gamepad in SDL HIDAPI Xbox 360 probe and exactly 1 GameController-backed gamepad in GameController probe. Current installed `--headless status` reports `identity: xone-hid`, `user-space: enabled`, `status: on (routes=1, active=none)`. `ForegroundConsumerCompatibilityDispatcherPool` keeps one shared dispatcher. | Pass | Verify again after routing changes. |
| Duplicate physical controllers in GUI | Multiple same-model controllers must not collapse into one SwiftUI identity or picker tag. | Found GUI identity was only `vendorID:productID:name`. Added `locationID` to `XPCDeviceDescription`, populate it from `DeviceManager`, and include serial/location in `DeviceViewModel.id`. `rtk test swift test` reports 274 tests passing. Current installed `--headless list` reports one GameSir-G7 SE controller, so two-device hardware proof is unavailable. | Fixed | Verify with two same-model controllers when hardware is available. |
| Same-model controller XPC queries | Input state, packet log, and physical rumble from the GUI must target the selected same-model controller, not the first VID/PID match. | Found XPC operations accepted only VID/PID. Added serial/location XPC overloads and `DeviceIdentifier.matchesQuery(_:)` with serial, then location, then legacy model fallback. `rtk test swift test` reports 277 tests passing; `./scripts/ojd rebuild-fast dev` rebuilt and relaunched installed app. Current installed `--headless list` reports one controller, so two-device hardware proof is unavailable. | Fixed | Verify with two same-model controllers when hardware is available. |
| USB fallback identity | USB fallback location IDs must use the documented `(bus << 16) | address` encoding to avoid stale/duplicate pipeline identity drift. | Found `DeviceManager` used `bus << 8`; added `DeviceIdentifier.usbLocationID(bus:address:)` and routed USB detection through it. `rtk test swift test` reports 275 tests passing. | Fixed | Recheck live reconnect behavior with multiple USB controllers when hardware is available. |
| Neutral controller state | Newly visible virtual gamepads must start neutral and remain neutral when no route is active. | Fresh after GetReport patch: `rtk ./scripts/ojd diag backends --seconds 2` reported `EXPECT_PASS: exactly one neutral OJD gamepad` for SDL HIDAPI and `EXPECT_PASS: exactly one neutral SDL gamepad` for GameController. Tests cover initial neutral and route neutralization. | Pass | Add focused route-switch runtime probe if source changes again. |
| Rumble behavior | Consumer-visible virtual gamepad rumble must still work. | Fresh after GetReport patch: `rtk ./scripts/ojd diag backends --seconds 2` reported `SDL_RumbleGamepad=ok` and `EXPECT_PASS: SDL_RumbleGamepad succeeded on 1 gamepad(s)` for both SDL HIDAPI and GameController probes. | Pass | Recheck after HID descriptor/report edits. |
| Native GameController behavior | The Apple GameController compatibility path must enumerate as a `GCController` and accept haptics. | `rtk ./scripts/ojd diag gamecontroller --seconds 5 --rumble`: virtual HID is `gamecontroller=yes`, connected as `Xbox One`, haptics localities reported, and `rumble: played`. | Pass | Browser Gamepad API remains separate and unproven. |
| Browser Gamepad API behavior | Browser `navigator.getGamepads()` controls must match the active identity table and not show stale/duplicate devices. | User-supplied Safari `https://hardwaretester.com/gamepad` evidence before fixes: Apple GameController mode appeared as `Xbox 360 Wired Controller Extended Gamepad`, all buttons/axes stayed 0, rumble worked; SDL2/3, Generic HID, and Xbox360 HID modes did not appear; Xbox One HID mode appeared, inputs did not work, rumble did not work. Installed status during Safari test showed `active=none` while rumble status recorded an app report, proving Safari can send rumble while route selection fails to activate input. Route fallback and GetReport current-state/report-ID fixes were installed, but user retest reported zero difference. Added telemetry for `dispatches`, `nonEmpty`, `events`, `writes`, `getReports`, `lastWrite`, and `lastGet`. Local installed `selftest` proves user-space report delivery reaches IOHID callbacks. Tried Xbox One S identity/report shape for `apple-gamecontroller`; local probe contradicted it (`045E:02EA "Xbox Wireless Controller" ... gamecontroller=no`, `Initial controllers: 0`), so the patch was reverted. Added RED/GREEN `VirtualControllerBackendTests/testAppleGameControllerIdentityUsesAcceptedXbox360IdentityWithSDLPacketShape` and changed Apple GameController to keep the accepted `045E:028E` Xbox 360 identity while using the 20-byte Xbox 360 SDL/HIDAPI packet shape. Focused tests passed. `./scripts/ojd rebuild-fast dev` installed the candidate. `rtk ./scripts/ojd diag gamecontroller --seconds 5 --rumble` shows `045E:028E "Xbox 360 Wired Controller" ... gamecontroller=yes`, connects as `Xbox One`, and `rumble: played`. Opened Safari to `https://hardwaretester.com/gamepad`; installed status after opening shows successful non-empty writes (`dispatches=21, nonEmpty=20, events=22, writes=21, getReports=0, lastWrite=0x0`) while `active=none`. User retest confirms Apple GameController inputs now work. User retest also confirms remaining failures: SDL2/3, Generic HID, and Xbox 360 HID still do not show up in Safari. For Xbox One HID, local `045E:02EA` was `gamecontroller=no`; the observed Apple/SDL bridge path was `045E:02E0` with version `0x5326`. Added RED/GREEN `VirtualControllerBackendTests.testCompatibilityProfileCatalog` coverage for `xone-hid` using `045E:02E0`, installed with `./scripts/ojd rebuild-fast dev`, switched to `xone-hid`, and `rtk ./scripts/ojd diag gamecontroller --seconds 5 --rumble` shows `045E:02E0 "Xbox One Wireless Controller" transport=Virtual gamecontroller=yes`, connects as Xbox One, and `rumble: played`. Installed status after Safari open/selftest shows non-empty user-space dispatch/write flow (`dispatches=203, nonEmpty=202, events=213, writes=203`). User retest confirms Xbox One HID inputs work in Safari. Safari rumble initially activated only physical LT; raw telemetry captured duplicate report ID bytes (`bytes=[03 0F 00 00 64 64 0A 00 00]`) while IOHID already reported `reportID=3`, so the Xbox One parser stripped the duplicate leading `0x03`. Focused parser tests passed and the installed build was refreshed. User retest confirms the whole controller now rumbles. User then reported Xbox One HID L3/R3 are swapped with left/right center buttons (View/Menu). Added xone-only button usage override: L3/R3 emit usages 9/10 and Menu/View emit usages 8/7 for Safari order; default raw Xbox One HID descriptor packing remains unchanged. `rtk test swift test --filter VirtualControllerBackendTests/testXboxOneCompatibilitySwapsCenterButtonsForSafariOrder`, `rtk test swift test --filter XboxOneHIDReportFormatTests`, and `./scripts/ojd rebuild-fast dev` passed. Later local reproduction showed remaining non-enumerating identities were rejected by GameController: Generic HID `4F4A:4449 ... gamecontroller=no`, SDL2/3 `4F4A:4448 ... gamecontroller=no`, and old x360 ASTRO `9886:0024 ... gamecontroller=no`. Added RED/GREEN `VirtualControllerBackendTests.testCompatibilityProfileCatalog` expectation that `x360-hid` uses the GameController-accepted `045E:028E` Xbox 360 wired identity instead of ASTRO C40. Installed with `./scripts/ojd rebuild-fast dev`; local probe now reports `045E:028E "Xbox 360 Wired Controller" transport=Virtual gamecontroller=yes`, connects as Xbox One, and `rumble: played`. Opened Safari to `https://hardwaretester.com/gamepad`. User later clarified "problem solved" applied to Xbox HID modes only, not Generic HID or SDL2/3. Added RED/GREEN `ScriptPackagingTests.testGameControllerDiagnosticsCanInjectSelfTest` and wired `./scripts/ojd diag gamecontroller --inject-selftest --inject-delay-ms N --inject-seconds N` through to the probe. `rtk ./scripts/ojd diag gamecontroller --seconds 5 --inject-selftest --inject-delay-ms 1000 --inject-seconds 2` on `x360-hid` reports `gamecontroller=yes`, a GC connect event, `GC_SELFTEST User-space: value 4, report 4`, and `GC_INPUT` callbacks, proving synthetic user-space input reaches GameController for an accepted identity. For Generic HID and SDL2/3, RED test `VirtualControllerBackendTests.testOJDOwnedIdentitiesUseBrowserAcceptedProfileInCompatibilityMode` failed because both still used OJD-owned VID/PID and the SDL 14-byte descriptor. Implemented browser-compatible fallback by making Generic HID and SDL2/3 compatibility profiles use the accepted `045E:028E` Xbox 360 wired identity plus `Xbox360SDLHIDReportFormat` and GamePad primary usage. Focused tests now pass. Installed with `./scripts/ojd rebuild-fast dev`. Local probes now show both `generic-hid` and `sdl2-3` as `045E:028E "Xbox 360 Wired Controller" transport=Virtual gamecontroller=yes`, connect as Xbox One, and play rumble; sdl2-3 selftest also reports `GC_SELFTEST User-space: value 4, report 4` and `GC_INPUT` callbacks. User manual confirmation: all backends now work fully in Safari Browser API. | Fixed | None. |
| Daemon health / UI lag | Daemon and GUI should not show high idle CPU or frontmost polling on MainActor. | Fresh process check after reinstall: daemon PID `91571` at 1.3% CPU, GUI PID `91580` at 0.0% CPU, both running from installed app/LoginItems paths. Installed `--headless status` connects over XPC and reports one device. Source cache removes `MainActor.run` from periodic foreground polling. | Pass | Sample daemon if CPU rises or code touches foreground monitor. |
| Permission state | UI/CLI must show daemon Input Monitoring and Accessibility separately. | Fresh installed `--headless status` shows Input Monitoring `granted`, Accessibility `granted`. Tests cover UI labels/status command strings. | Pass | Validate permission prompt command interactively only when user wants TCC panes reopened. |
| Permission prompt command flow | Headless permission request must target the daemon helper, register the bundled daemon app, and request both Input Monitoring and Accessibility without misattributing prompts to the GUI app. | Source audit: `CLI.swift` routes `permissions` to `PermissionsCommand`; `PermissionsCommand.swift` requires `request-daemon`, registers `DaemonManager.bundledDaemonApplicationURL`, launches `OpenJoystickDriverDaemon` with `--request-input-monitoring` and `OJD_PERMISSION_PROMPT_ONLY=1`, then opens `Privacy_ListenEvent` and `Privacy_Accessibility`. Daemon `main.swift` enters prompt helper mode for the flag/env. Installed binary prints `Usage: OpenJoystickDriver --headless permissions request-daemon`; help lists the command. `rtk test swift test --filter InputMonitoringPromptFlowTests/testHeadlessPermissionsCommandRequestsDaemonOwnedPrompt` passed. | Pass | Do not rerun the interactive prompt flow unless the user explicitly wants System Settings/TCC panes opened. |
| Stale DriverKit process | Active DriverKit process should run from installed sysext path, not old copy. | Rechecked with `ps -p 731 -o pid,ppid,pcpu,pmem,comm,args`: PID `731` still runs from `/Library/SystemExtensions/E8A12184-...`. Backend diagnostics previously expected `/Library/SystemExtensions/E482AF08-...`. Repair command cannot complete in noninteractive session because `sudo` needs password. User stated reboot items do not count here and cannot reboot. | Blocked | Only count this if interactive admin repair is allowed; otherwise do not loop on it as audit progress. |
| Backend diagnostics exit code | The backend acceptance loop must fail its process when critical diagnostics fail, so stale DriverKit or consumer regressions are not masked by a green command. | Found `diag backends` continued with `|| true` and exited 0 even when DriverKit diagnostics failed. Added failure accumulation for DriverKit, SDL HIDAPI, and GameController probe steps. Fresh `rtk ./scripts/ojd diag backends --seconds 2` exits 1 while still running all probes; the only failure reported is stale DriverKit process path mismatch, while SDL HIDAPI and GameController consumer probes pass duplicate/neutral/rumble expectations. | Fixed | After stale DriverKit is cleared, rerun backend diagnostics and require exit 0. |
| Backend diagnostics state preservation | Backend diagnostics must not leave Compatibility identity, user-space enablement, or output route changed after consumer probes. | `ScriptPackagingTests.testBackendDiagnosticsConfigureConsumerSpecificRoutes` covers `restore_ojd_route`. After fresh backend diagnostics, installed CLI reports `identity: xone-hid`, `user-space: enabled`, `output: secondaryOnly`, matching the pre-diagnostic route state. | Pass | Recheck after any diagnostic route edits. |
| Repository validation gates | Touched profiles, scripts, Swift code, localization, diagnostics, and runtime paths must pass the project-required focused checks. | Fresh gates passed after version/docs/CI updates: `swiftlint lint` found 0 violations in 151 files; `rtk ./scripts/ojd validate profiles` validated 29 controller profiles and 2 device schemas; `rtk err bash -n scripts/ojd scripts/ojd-*.sh` completed successfully with no errors; `rtk test swift test` completed successfully; `git diff --check` passed. | Pass | Rerun after any code/script/profile changes. |
| Worktree ownership | Preserve user/unrelated work and do not stage hidden changes. | `git diff --cached --name-only` returned empty. Worktree has many modified files plus untracked `AUDIT_PROGRESS.md` and `Sources/OpenJoystickDriver/Commands/PermissionsCommand.swift`. | Pass | Keep checking cached index before staging/commit actions. |

## Current open risks

1. Stale DriverKit PID 731 remains until interactive administrator repair is allowed; reboot is not an actionable item for this audit.
2. Browser Gamepad API objective is complete: Apple GameController, Generic HID, SDL2/3, Xbox 360 HID, and Xbox One HID all work fully per local probes plus user Safari confirmation.
3. CI/CD workflow logic now includes shell syntax validation in CI and release jobs; action pins and runner labels were audited as current.
4. Installed app permission command flow is source/test/help verified; interactive prompt panes are intentionally not reopened unless requested.
5. Two same-model physical controllers are not available in this session (`--headless list` reports one controller), so GUI duplicate-row and exact-query behavior is covered by code/tests but not hardware-proven.

## Next ordered audit steps

1. Do not spend further audit turns on stale DriverKit unless interactive admin repair becomes available.
2. Do not retest solved Browser API scope unless regression is reported.
3. Continue ledger-driven audit from remaining open risks; do not add new scope without a ledger row.
